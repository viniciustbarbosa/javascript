const numbers = [];

for (let i = 1; i <= 10; i++) {
    numbers.push(i); // Com o push, adicionamos o valor de i ao final do array numbers a cada iteração do loop.
}

console.log(numbers);

const frase = ["Olá", "mundo", "!"];

for (i = 0; i < frase.length ; i++) {
    console.log(frase[i]); // Acessamos cada elemento do array frase usando o índice i, que varia de 0 a frase.length - 1, e imprimimos cada palavra na tela.
}